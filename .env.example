# Nur fuer `docker compose config` in der GitHub Action. Die echten Werte leben
# als Environment Variables an der Coolify-App, nicht hier.
PAPERLESSMCP_VERSION=latest
PAPERLESS_API_TOKEN=example
PAPERLESSMCP_CF_DNS_API_TOKEN=example
